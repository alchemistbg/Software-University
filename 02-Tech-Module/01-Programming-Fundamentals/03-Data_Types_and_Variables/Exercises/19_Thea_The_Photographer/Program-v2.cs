using System;

namespace _19_Thea_The_Photographer
{
	class Program_v2
	{
		//static void Main(string[] args)
		//{
		//	ulong pictureNumber = ulong.Parse(Console.ReadLine());
		//	ulong filterTime = ulong.Parse(Console.ReadLine());
		//	ulong filterFactor = ulong.Parse(Console.ReadLine());
		//	ulong uploadTime = ulong.Parse(Console.ReadLine());

		//	ulong timeToFilter = pictureNumber * filterTime;

		//	ulong selectedPictures = (ulong)Math.Ceiling(pictureNumber * (filterFactor * 0.01));
		//	ulong timeToUpload = selectedPictures * uploadTime;
		//	ulong totalSec = timeToFilter + timeToUpload;

		//	TimeSpan time = TimeSpan.FromSeconds(totalSec);
		//	string timeFormat = @"d\:hh\:mm\:ss";
		//	Console.WriteLine(time.ToString(timeFormat));
		//}
	}
}
