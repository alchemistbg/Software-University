# I cannot test this solution, because the problem doesn't exist in the judge system
# My solution is cleaner. It does not produce empty row

import triangle_module

n = int(input())
triangle_module.print_triangle(n + 1)
